# Fashion Marketplace Outfit Builder

This workspace is scaffolded for a MERN-based Outfit Builder module.

## Structure
- `client`: React frontend
- `server`: Node.js and Express backend
- `docs/outfit-builder`: architecture and API contract

## Next Steps
1. Install dependencies in `client` and `server`.
2. Add the React builder screens and Redux state.
3. Add the Express routes, controllers, models, and services.
4. Wire the rule-based recommendation and compatibility scoring flow.
