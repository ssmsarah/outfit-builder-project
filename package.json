{
  "name": "ecommerce-outfit-builder",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "npm run dev --workspace server",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace server"
  },
  "dependencies": {
    "lucide-react": "^1.47.0",
    "mongodb": "^7.5.0"
  }
}
